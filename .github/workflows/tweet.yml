name: Tweet every day

on:
  schedule:
    - cron: '30 3 * * *'   # 09:00 AM IST
    - cron: '30 6 * * *'   # 12:00 PM IST
    - cron: '30 9 * * *'   # 03:00 PM IST
  workflow_dispatch:

jobs:
  tweet:
    runs-on: ubuntu-latest

    steps:
      - name: 🔗 Call /tweet endpoint
        run: |
          echo "📡 Calling tweet endpoint..."
          curl --fail --location --max-time 20 https://x-auto-ochre.vercel.app/tweet
